apiVersion: v1
kind: Service
metadata:
  name: morphic-community-manager
spec:
  type: ClusterIP
  selector:
    app: morphic-community-manager
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
